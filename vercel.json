{
    "buildCommand": "npm install && npm run build",
    "outputDirectory": "client/dist",
    "framework": "react",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/backend/server.js"
        }
    ]
}